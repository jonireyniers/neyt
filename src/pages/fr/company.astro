---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import companyData from '../../data/company.json';
import type { CompanyData, Company } from '../../types/company';

const locale: 'en' | 'fr' | 'nl' = 'fr';
const company = (companyData as CompanyData).company as Company;
const closingDates = ((companyData as CompanyData).closingDates) ?? [];
---

<BaseLayout title={`${company.names[locale]} — Entreprise`} locale={locale}>
  <Header locale={locale} />

  <main>
    <!-- Hero Section -->
    <section class="relative py-20 bg-gradient-to-br from-primary to-secondary text-white">
      <div class="container flex justify-center items-center">
        <img 
          src="/images/furniture/logobackground.png" 
          alt="Neyt Meubles" 
          class="h-24 md:h-32 lg:h-40 w-auto animate-fade-in" 
        />
      </div>
    </section>

    <!-- About Section -->
    <section class="section">
      <div class="container">
        <div class="grid md:grid-cols-2 gap-12 items-center">
          <div>
            <h2 class="text-3xl font-serif font-bold mb-6">Notre Histoire</h2>
            
            <div class="space-y-4 text-gray-700">
              <p class="text-lg">
                NEYT est une <strong>entreprise familiale belge</strong> avec une usine de production à Beervelde, 
                spécialisée dans la production de meubles modernes en kit depuis plus de 50 ans.
              </p>

              <p>
                Des machines très modernes et de haute technologie ainsi qu'une automatisation extensive 
                nous permettent de produire des meubles modernes à des prix très compétitifs, qui peuvent 
                être vendus dans le monde entier.
              </p>

              <p>
                Notre gamme comprend des meubles de chambre à coucher pour bébés, adolescents et adultes, 
                des meubles de salle à manger/salon et des meubles de bureau.
              </p>
            </div>

            <div class="mt-8 grid grid-cols-3 gap-4 text-center">
              <div class="bg-accent/10 rounded-lg p-4">
                <div class="text-3xl font-bold text-accent mb-1">50+</div>
                <div class="text-sm text-gray-600">Ans d'Expérience</div>
              </div>
              <div class="bg-accent/10 rounded-lg p-4">
                <div class="text-3xl font-bold text-accent mb-1">B2B</div>
                <div class="text-sm text-gray-600">Modèle Commercial</div>
              </div>
              <div class="bg-accent/10 rounded-lg p-4">
                <div class="text-3xl font-bold text-accent mb-1">🇧🇪</div>
                <div class="text-sm text-gray-600">Fabriqué en Belgique</div>
              </div>
            </div>
          </div>

          <div class="relative">
            <div class="bg-white rounded-lg shadow-2xl overflow-hidden transform hover:scale-105 transition-transform duration-300">
              <div class="aspect-[4/3] bg-gray-100">
                <img 
                  src="/images/furniture/company.jpg" 
                  alt="Usine de production NEYT" 
                  class="w-full h-full object-cover" 
                  loading="lazy" 
                />
              </div>
            </div>
            <div class="absolute -bottom-6 -left-6 w-32 h-32 bg-accent/20 rounded-full -z-10"></div>
            <div class="absolute -top-6 -right-6 w-24 h-24 bg-primary/20 rounded-full -z-10"></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact & Info Section -->
    <section class="section bg-gray-50">
      <div class="container">
        <div class="grid md:grid-cols-1 gap-8">
          <!-- Closing Dates Card -->
          {closingDates.length > 0 && (
            <div class="bg-white rounded-xl shadow-lg p-8 hover:shadow-xl transition-shadow">
              <div class="flex items-center gap-3 mb-6">
                <div class="w-12 h-12 bg-accent/10 rounded-full flex items-center justify-center">
                  <svg class="w-6 h-6 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                </div>
                <h3 class="text-2xl font-semibold">Périodes de Fermeture {closingDates[0].year}</h3>
              </div>

              <ul class="space-y-3">
                {closingDates[0].periods.map((period: string) => (
                  <li class="flex items-start gap-3 text-gray-700">
                    <span class="text-accent text-xl leading-none mt-0.5">•</span>
                    <span>{period}</span>
                  </li>
                ))}
              </ul>

              <div class="mt-6 p-4 bg-amber-50 border-l-4 border-amber-500 rounded">
                <p class="text-sm text-gray-700">
                  <strong class="text-amber-700">Note:</strong> Veuillez planifier vos commandes en conséquence. 
                  Aucune expédition ne sera traitée pendant ces périodes.
                </p>
              </div>
            </div>
          )}
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="section bg-gradient-to-br from-primary to-secondary text-white">
      <div class="container text-center">
        <h2 class="text-3xl md:text-4xl font-serif font-bold mb-8">
          Intéressé par Nos Produits?
        </h2>
        <div class="flex gap-4 justify-center">
          <a 
            href="/fr/contact" 
            class="inline-flex items-center gap-2 bg-white text-primary px-8 py-4 rounded-lg font-semibold hover:bg-gray-100 transition-all transform hover:scale-105 shadow-lg"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
            </svg>
            Contactez-Nous
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer locale={locale} />
</BaseLayout>