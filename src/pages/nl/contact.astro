---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const locale: 'en' | 'fr' | 'nl' = 'nl';
---

<BaseLayout title="Contact — Neyt" locale={locale}>
  <Header locale={locale} />

  <main>
    <!-- Hero Section -->
    <section class="relative py-16 bg-gradient-to-br from-primary to-secondary text-white">
      <div class="container">
        <div class="max-w-3xl mx-auto text-center">
          <h1 class="text-4xl md:text-5xl font-serif font-bold mb-4">Neem Contact Op</h1>
          <p class="text-lg md:text-xl opacity-90">We horen graag van u. Stuur ons een bericht en we antwoorden zo snel mogelijk.</p>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section class="section">
      <div class="container max-w-6xl">
        <div class="grid lg:grid-cols-2 gap-12">
          <!-- Contact Info -->
          <div class="space-y-8">
            <div>
              <h2 class="text-2xl font-serif font-bold mb-6">Contactinformatie</h2>
              
              <div class="space-y-6">
                <!-- Address Card -->
                <div class="flex items-start gap-4 p-6 bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow">
                  <div class="w-12 h-12 bg-accent/10 rounded-full flex items-center justify-center flex-shrink-0">
                    <svg class="w-6 h-6 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                    </svg>
                  </div>
                  <div>
                    <h3 class="font-semibold text-lg mb-2">Ons Kantoor</h3>
                    <address class="not-italic text-gray-600">
                      <div class="font-medium text-gray-900">Neyt NV</div>
                      <div>Heistraat 12</div>
                      <div>9080 Beervelde</div>
                      <div>België</div>
                    </address>
                  </div>
                </div>
              </div>
            </div>

            <!-- B2B Notice -->
            <div class="p-6 bg-blue-50 border-l-4 border-blue-500 rounded-lg">
              <div class="flex gap-3">
                <svg class="w-6 h-6 text-blue-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <div>
                  <h3 class="font-semibold text-blue-900 mb-1">Alleen B2B</h3>
                  <p class="text-sm text-blue-800">
                    We verkopen niet rechtstreeks aan het publiek. Voor informatie over Neyt-producten, 
                    neem contact op met een meubelzaak in uw omgeving.
                  </p>
                </div>
              </div>
            </div>
          </div>

          <!-- Contact Form -->
          <div class="bg-white rounded-2xl shadow-xl p-8 lg:p-10">
            <h2 class="text-2xl font-serif font-bold mb-6">Stuur ons een Bericht</h2>

            <form id="contactForm" class="space-y-5" method="post" action="/api/contact" novalidate>
              <div class="grid md:grid-cols-2 gap-5">
                <div>
                  <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
                    Volledige Naam <span class="text-red-500">*</span>
                  </label>
                  <input 
                    id="name" 
                    name="name" 
                    required 
                    class="w-full border border-gray-300 px-4 py-3 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition" 
                    placeholder="Jan Janssen"
                  />
                </div>

                <div>
                  <label for="company" class="block text-sm font-medium text-gray-700 mb-2">
                    Bedrijf <span class="text-red-500">*</span>
                  </label>
                  <input 
                    id="company" 
                    name="company" 
                    required 
                    class="w-full border border-gray-300 px-4 py-3 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition" 
                    placeholder="Uw Bedrijf"
                  />
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-5">
                <div>
                  <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
                    E-mail <span class="text-red-500">*</span>
                  </label>
                  <input 
                    id="email" 
                    name="email" 
                    type="email" 
                    required 
                    class="w-full border border-gray-300 px-4 py-3 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition" 
                    placeholder="jan@bedrijf.nl"
                  />
                </div>

                <div>
                  <label for="country" class="block text-sm font-medium text-gray-700 mb-2">
                    Land <span class="text-red-500">*</span>
                  </label>
                  <select 
                    id="country" 
                    name="country" 
                    required 
                    class="w-full border border-gray-300 px-4 py-3 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition"
                  >
                    <option value="">Selecteer een land</option>
                    <option value="BE">België</option>
                    <option value="NL">Nederland</option>
                    <option value="FR">Frankrijk</option>
                    <option value="DE">Duitsland</option>
                    <option value="UK">Verenigd Koninkrijk</option>
                    <option value="OTHER">Overig</option>
                  </select>
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-5">
                <div>
                  <label for="activity" class="block text-sm font-medium text-gray-700 mb-2">
                    Activiteit
                  </label>
                  <input 
                    id="activity" 
                    name="activity" 
                    class="w-full border border-gray-300 px-4 py-3 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition" 
                    placeholder="Meubelhandel"
                  />
                </div>

                <div>
                  <label for="city" class="block text-sm font-medium text-gray-700 mb-2">
                    Stad
                  </label>
                  <input 
                    id="city" 
                    name="city" 
                    class="w-full border border-gray-300 px-4 py-3 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition" 
                    placeholder="Brussel"
                  />
                </div>
              </div>

              <div>
                <label for="message" class="block text-sm font-medium text-gray-700 mb-2">
                  Bericht <span class="text-red-500">*</span>
                </label>
                <textarea 
                  id="message" 
                  name="message" 
                  rows="5" 
                  required 
                  class="w-full border border-gray-300 px-4 py-3 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition resize-none" 
                  placeholder="Vertel ons over uw aanvraag..."
                ></textarea>
              </div>

              <div class="flex items-center justify-between pt-2">
                <p class="text-sm text-gray-500">
                  <span class="text-red-500">*</span> Verplichte velden
                </p>
                <button 
                  type="submit" 
                  class="inline-flex items-center gap-2 px-6 py-3 bg-accent text-white rounded-lg font-semibold hover:bg-accent/90 focus:outline-none focus:ring-2 focus:ring-accent focus:ring-offset-2 transition-all transform hover:scale-105 shadow-lg"
                >
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
                  </svg>
                  Verzend Bericht
                </button>
              </div>

              <div id="status" class="text-sm min-h-[1.5rem]"></div>
            </form>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer locale={locale} />

  <script>
    const form = document.getElementById('contactForm') as HTMLFormElement;
    const statusEl = document.getElementById('status') as HTMLElement;

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      statusEl.textContent = 'Verzenden...';
      statusEl.className = 'text-sm min-h-[1.5rem] text-blue-600';

      const data = new FormData(form);

      try {
        const resp = await fetch('/api/contact', { method: 'POST', body: data });
        const json = await resp.json();

        if (resp.ok && json?.success) {
          statusEl.textContent = '✓ Bericht succesvol verzonden! We nemen binnenkort contact met u op.';
          statusEl.className = 'text-sm min-h-[1.5rem] text-green-600 font-medium';
          form.reset();
        } else {
          statusEl.textContent = `✗ ${json?.error || 'Er ging iets mis. Probeer het opnieuw.'}`;
          statusEl.className = 'text-sm min-h-[1.5rem] text-red-600 font-medium';
        }
      } catch (err) {
        statusEl.textContent = '✗ Verbinding mislukt. Controleer uw netwerk.';
        statusEl.className = 'text-sm min-h-[1.5rem] text-red-600 font-medium';
      }
    });
  </script>
</BaseLayout>